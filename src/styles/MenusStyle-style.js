import styled, { css } from "styled-components";
import Container from "./Container";

const MenusStyle = styled(Container)`
    ${({ theme }) => {
    return css`
    `;
}}
`;

const styledMainComponents = { MenusStyle };

export default styledMainComponents;